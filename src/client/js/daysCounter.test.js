import { getDaysLeft  } from './daysCounter';
import { getTripLenght } from "./daysCounter";

describe('getDaysLeft function', () => {
  test('It should return true', () => {
    expect(getDaysLeft).toBeDefined();
  });
});

describe('getTripLenght function', () => {
  test('It should return true', () => {
    expect(getTripLenght).toBeDefined();
  });
});